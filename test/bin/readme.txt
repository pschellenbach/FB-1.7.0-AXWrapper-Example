test/bin

The compiled DLL & OCX files are included for convenience. To use the
pre-compiled DLL & OCX files, open an admin command prompt, CD to this
directory and use the regsvr32 command to register the DLL & OCX files.
